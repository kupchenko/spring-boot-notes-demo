const developmentProps = {
    API_URL_BASE: 'http://localhost',
    API_CONTEXT_PATH: '/api',
    APP_OAUTH2_USERNAME: 'notes-react-client',
    APP_OAUTH2_PASSWORD: 'secret'
};

const appConfig = {
    ...developmentProps
};

export default appConfig;
